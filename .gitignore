# ML model artifacts live in ./models at the repo root. Anchor with a leading
# slash so this does NOT also ignore app/models/ (the Pydantic schemas package).
/models/
venv/
.venv/
__pycache__/
*.env
*.env.*
# commit the template; real .env* stay ignored
!.env.free.example
*.log
eval/reports/
app/rag/data/ingested/
.claude/
